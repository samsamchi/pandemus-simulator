{
  "name": "server",
  "version": "1.0.0",
  "description": "Pandemus backend",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "node ./src/index.js",
    "db:deploy": "npx prisma migrate deploy && npx prisma generate"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@prisma/client": "^6.7.0",
    "express": "^5.1.0",
    "express-async-handler": "^1.2.0",
    "express-validator": "^7.2.1"
  },
  "devDependencies": {
    "prisma": "^6.7.0"
  }
}
